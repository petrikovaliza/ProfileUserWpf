<Window x:Class="ProfilUserBurdinaPetrikova.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProfilUserBurdinaPetrikova"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
         </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
    
        <Label BorderThickness="4" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
               Content="Мой профиль" HorizontalAlignment="Center"
               FontSize="16" FontWeight="Bold" Margin="10">
                <Label.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="LightBlue" Offset="0.0"/>
                        <GradientStop Color="#FFDDACF3" Offset="1.0"/>
                    </LinearGradientBrush>
                </Label.Background>
            
        </Label>

        <StackPanel Grid.Row="1" Grid.Column="0" Margin="10" Background="#FFDAC6C6">
            <Label Content="Имя:"/>
            <TextBox x:Name="NameTextBox" Text="Алексей" TextChanged="NameTextBox_TextChanged"/>

            <Label Content="Возраст:"/>
            <Slider x:Name="AgeSlider" Minimum="0" Maximum="100" Value="25"
            TickFrequency="1" IsSnapToTickEnabled="True" ValueChanged="AgeSlider_ValueChanged"/>
            <TextBlock Text="{Binding ElementName=AgeSlider, Path=Value, StringFormat='Лет: {0:F0}'}"
                HorizontalAlignment="Center"/>
            <Label Content="Город:"/>
            <ComboBox x:Name="CityCombobox" SelectedIndex="0" SelectionChanged="CityCombobox_SelectionChanged">
                <ComboBox.Background>
                    <LinearGradientBrush EndPoint="0,1" StartPoint="0,0">
                        <GradientStop Color="#FFF0F0F0" Offset="0"/>
                        <GradientStop Color="#FFBA92CD" Offset="1"/>
                    </LinearGradientBrush>
                </ComboBox.Background>
                <ComboBoxItem Content="Москва"/>
                <ComboBoxItem Content="Санкт-Петербург"/>
                <ComboBoxItem Content="Тюмень"/>
            </ComboBox>

            <CheckBox x:Name="SubscribeCheckBox" Content="Подписаться на рассылку"
                        IsChecked="True" Margin="0 10 0 0" Checked="SubscribeCheckBox_Checked"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10" Background="#FFDAC6C6">
            <TextBlock x:Name="OutputTextBLock" TextWrapping="Wrap">
            </TextBlock>
            <Image x:Name="UserImage" Source="/image/user.png"
            Width="100" Height="100" Margin="0 10 0 0"
                    HorizontalAlignment="Left" Stretch="None"/>
        </StackPanel>
        <Button BorderThickness="4" BorderBrush="Chocolate" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                    Content="Обновить профиль" Click="UpdateProfileBurron_Click"
                    Margin="10" Width="120" Height="30" Background="#FFF6D3F9" RenderTransformOrigin="0,0.5">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6" />
                </Style>
            </Button.Resources>
        </Button> 
    </Grid>
</Window>
